# То же, что gte_pool_diag_k500.yaml, но пул top-1000. Смысл пары — не одно
# число, а форма кривой: если между 500 и 1000 прирост title EM@pool уже
# плоский, дальше расширять пул бесполезно и узкое место не в его размере.
#
# steps: 1 по той же причине, что и у k500: в режиме fixed пул один на все
# шаги, а report читает только первый хоп. Стадии judge и score не нужны,
# запускать как `--only retrieve`.
label: GTE пул top-1000 (диагностика)
slug: gte-pool-diag-k1000

order: 902
hypothesis: >-
  Расширение пула до 1000 кандидатов показывает, насыщается ли title EM@pool:
  плоский участок между 500 и 1000 означает, что размер пула — не тот рычаг.

retrieve:
  kind: qrag
  index_dir: /home/a.anokhin/Judge/datasets/data_sources/full-wiki/wiki18-qrag-jul21-best-raw
  candidate_index_dir: /home/a.anokhin/Judge/datasets/data_sources/full-wiki/wiki18-gte
  candidate_backend: torch-cuda
  qrag_repo: /home/a.anokhin/Judge/Q-RAG-feedback
  input: /home/a.anokhin/Judge/datasets/data_sources/hotpotqa/hotpot_dev_fullwiki_v1.json
  cuda_visible_devices: "6"
  device: cuda:0
  model_dtype: float32
  state_source: critic
  batch_size: 64

  top_k: 1000
  steps: 1

  mode: fixed
  reranker: none
  log_candidates: full

  dedupe_titles: true
  trust_remote_code: true
  auto_prepare: false

judge:
  script: /home/a.anokhin/Judge/Q-RAG-feedback/answer_judge_llms.py
  base_url: http://127.0.0.1:8010/v1
  answer_model: Qwen3-4B
  max_tokens: 1000
  judge_max_tokens: 100
  shards: 32
