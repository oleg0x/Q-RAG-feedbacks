defaults:
  - algo: pqn_e5_hotpotqa
  - envs: hotpotqa
  - feedback: llm
  - logger: logging
  - _self_

seed: 42
device: cuda
learning_start: 200
steps_count: 30000
batch_size: 64
accumulate_grads: 2
eval_interval: 100
eval_episodes: 500
envs_parallel: 1
max_action_length: 128
max_action_length_in_memory: 64

feedback:
  llm_answer:
    task: HotPotQA

