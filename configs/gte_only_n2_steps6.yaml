# То же, что gte_only_steps6.yaml, но титул закрывается не первым взятым
# чанком, а вторым: --max-chunks-per-title 2.
#
# Зачем. Нарезка Wiki-18 идёт по 100 слов и не уважает границы предложений,
# поэтому gold-предложение регулярно оказывается не в том чанке статьи,
# который выше в ранжировании GTE: у 1 377 из 9 794 найденных титулов в
# top-100 есть чанк лучше выбранного. Жёсткая дедупликация делает его
# недостижимым навсегда. Квота в два чанка его открывает — но ценой одного
# титула из бюджета, а вопросы двухоповые.
#
# Отсюда и бюджет: на steps 2 проверять нечего, два чанка, потраченные на
# одну статью, для bridge-вопроса заведомо хуже. Смысл появляется там, где
# бюджета хватает на оба gold-титула и ещё остаётся.
label: GTE top-6, 2 чанка на титул
slug: gte-only-n2-steps6

order: 46
hypothesis: >-
  Разрешение брать второй чанк той же статьи достаёт gold-предложения,
  запертые дедупликацией, и это перевешивает потерю одного титула из
  бюджета.

retrieve:
  kind: qrag
  index_dir: /home/a.anokhin/Judge/datasets/data_sources/full-wiki/wiki18-qrag-jul21-best-raw
  candidate_index_dir: /home/a.anokhin/Judge/datasets/data_sources/full-wiki/wiki18-gte
  candidate_backend: torch-cuda
  qrag_repo: /home/a.anokhin/Judge/Q-RAG-feedback
  input: /home/a.anokhin/Judge/datasets/data_sources/hotpotqa/hotpot_dev_fullwiki_v1.json
  cuda_visible_devices: "1"
  device: cuda:0
  model_dtype: float32
  state_source: critic
  batch_size: 64
  top_k: 100
  steps: 6
  mode: fixed
  reranker: none
  log_candidates: none

  dedupe_titles: true

  # Единственное отличие от gte_only_steps6.yaml.
  max_chunks_per_title: 2

  trust_remote_code: true
  auto_prepare: false

judge:
  script: /home/a.anokhin/Judge/Q-RAG-feedback/answer_judge_llms.py
  base_url: http://127.0.0.1:8010/v1
  answer_model: Qwen3-4B
  max_tokens: 1000
  judge_max_tokens: 100
  shards: 32
